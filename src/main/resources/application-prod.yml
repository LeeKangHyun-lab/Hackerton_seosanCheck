server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: com.mysql.cj.jdbc.Driver

  session:
    store-type: jdbc
    jdbc:
      initialize-schema: never

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    org.mybatis: DEBUG
    hackerton.seosancheck.mapper: DEBUG

jwt:
  secret: ${JWT_SECRET}
  access-expiry: 900000      # 15분
  refresh-expiry: 1209600000 # 14일

openai:
  api-key: ${OPENAI_API_KEY}

weather:
  api:
    key: "1Sl0GJ1xR%2B%2FbnOgwQpif8IlyjMiaByjrn%2BBf%2BepIiiIplgh1efozoLNtOUdRIVuKVfYJJsBHbkffb%2Ba9WafcNQ%3D%3D"